export enum UserRoleEnum {
  ADMIN = 'admin',
  MANAGER = 'manager',
  STAFF = 'staff',
}

export enum DifficultyLevelEnum {
  EASY = 'easy',
  MEDIUM = 'medium',
  HARD = 'hard',
  EXPERT = 'expert',
}

export enum BookingTypeEnum {
  QUEST = 'quest',
  CAFE = 'cafe',
}

export enum BookingStatusEnum {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed',
}

export enum MenuItem {
  BOOKINGS = 'bookings',
  TABLE_GRID = 'table-grid',
  QUEST_GRID = 'quest-grid',
  CONTENT = 'content',
  REFERENCE = 'reference',
  EMPLOYEES = 'employees',
}
